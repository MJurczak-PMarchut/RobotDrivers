/*
 * WProgram.h
 *
 * Pre-1.0 Arduino header. Adafruit_GFX.h falls back to this when its
 * `#if ARDUINO >= 100` check fails; we force that branch via the ARDUINO
 * compile definition (see vscode_generated.cmake) so this is a safety net,
 * not the primary path - kept for completeness/robustness.
 */
#ifndef ROBOTDRIVERS_OLEDDRIVERS_ARDUINOCOMPAT_WPROGRAM_H_
#define ROBOTDRIVERS_OLEDDRIVERS_ARDUINOCOMPAT_WPROGRAM_H_

#include "Arduino.h"
#include "Print.h"

#endif /* ROBOTDRIVERS_OLEDDRIVERS_ARDUINOCOMPAT_WPROGRAM_H_ */
